cask_args appdir: "/Applications"

# additional repositories to tap into
tap "homebrew/bundle"
tap "homebrew/cask"
tap "homebrew/core"

# cli packages to install
brew "git"
brew "node"
brew "wget"

# these are needed for "cowsay | fortune" ;)
brew "cowsay"
brew "fortune"

# gui applications to install
cask "spotify"
cask "visual-studio-code"
cask "steam"
cask "discord"
cask "virtualbox"
cask "firefox"
